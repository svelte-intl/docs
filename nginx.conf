server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    # Strip trailing slash (e.g. /docs/ → /docs) so try_files can match /docs.html
    rewrite ^/(.+)/$ /$1 permanent;

    location / {
        try_files $uri $uri.html $uri/ =404;
    }
}
