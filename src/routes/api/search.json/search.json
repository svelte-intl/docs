[{"title":"Getting Started","href":"/docs/getting-started","description":"A quick guide to get started with svelte-i18n","content":"import { Callout, Tabs, TabItem, Steps, Step } from \"@svecodocs/kit\";   The following guide will walk you through the process of getting svelte-i18n up and running.  Configuration   Install the package  You can install the project via npm or pnpm. npm install @svelte-i18n/core # pnpm add @svelte-i18n/core  Create i18n.ts  Create a new file called i18n.ts inside src/lib to initialize svelte-i18n.  import { createI18n } from '$lib/i18n'; import nl from './locales/nl.json'; import en from './locales/en.json';  // In this example we load the dictionaries using an import // But its also possible to load the dictionary from a async resource export const { i18n, useI18n } = await createI18n({     locales: ['nl', 'en'],     locale: 'en',     dictionaries: {         nl,         en     } });  Edit +layout.svelte  To make svelte-i18n SSR safe we wrap our app with a context provider.  This step is required even if you dont use SSR, since we use the $2.       import { I18nContext } from '@svelte-i18n/core';     import { i18n } from '$lib/i18n';      let { children } = $props();        {@render children?.()}   🎉 finished, you have setup svelte-i18n       import { useI18n } from '$lib/i18n';      // ...     let user = $state('Richard');     let { t } = useI18n();   {t(\"Welcome, {user}\", { user })}"},{"title":"Introduction","href":"/docs/index","description":"Why another i18n library?","content":"import { Callout } from '@svecodocs/kit'   ✅ Easy to use ✅ Easiest setup we can come up with ✅ Safe to use with SSR by default ✅ No external dependencies ✅ Very small footprint (8kb)  Motivation  Most Svelte-specific i18n libraries are outdated and/or rely on Svelte stores instead of runes. That approach works, but I did not like the $_('hello') syntax and wanted something more future-proof.  Many libraries also require a complicated setup, and in the case of $2, a lot of configuration. But what if you just need i18n and nothing more? That is exactly why I created svelte-i18n. No deep nesting, just a simple key -> value translation dictionary  {     \"Hello, {username}!\": \"Hello, {username}!\",     \"welcome_banner\": \"Welcome to our website\"     ... }  Svelte-i18n is SSR safe by default, since we are using the $2     Parameter types are inferred from the key string, not the translated value.  Keys like \"Hello, \\{username\\}!\" are fully type-safe, but opaque keys like  \"welcome_banner\" won't have typed parameters even if the value contains placeholders.    This library has full TypeScript support, your translation keys are autocompleted when you call t(\"Hello, {username}!\", { username: 'Richard' }),  and if a message contains variables like {username}, TypeScript will enforce that you pass the correct arguments.       import { useI18n } from '$lib/i18n';      const { t } = useI18n();   {t('Hello, {username}!', { username: 'Richard' })} `"},{"title":"Dictionaries","href":"/docs/miscellaneous/dictionaries","description":"Learn what a dictionary is","content":"import { Callout, Tabs, TabItem, Steps, Step } from \"@svecodocs/kit\";  Dictionaries   A book or electronic resource that lists the words of a language (typically in alphabetical order) and gives their meaning   In svelte-i18n, a dictionary is a flat JSON object containing key-value pairs, where each key represents a phrase or identifier and its value is the translated string. Nesting is not supported — the Dictionary type is strictly Record, so all keys and values must be plain strings at the top level. If your project requires nested structures, consider using a different library.  The structure shown below illustrates how your JSON object should be formatted.      {     \"Welcome, {username}!\": \"Welcome, {username}!\",     \"Subscribe\": \"Subscribe\",     \"Your account has a balance of {count} USD\": \"Your account has a balance of {count} USD\",     \"You have used {count} out of {total} credits\": \"You have used {count} out of {total} credits\",     // ... }     {     \"Welcome, {username}!\": \"Hoi, {username}!\",     \"Subscribe\": \"Abonneren\",     \"Your account has a balance of {count} USD\": \"Je account heeft een saldo van {count} USD\",     \"You have used {count} out of {total} credits\": \"Je hebt {count} van de {total} credits gebruikt\",     // ... }     {     \"Welcome, {username}!\": \"مرحبًا، {username}!\",     \"Subscribe\": \"اشترك\",     \"Your account has a balance of {count} USD\": \"حسابك يحتوي على رصيد قدره {count} دولار أمريكي\",     \"You have used {count} out of {total} credits\": \"لقد استخدمت {count} من أصل {total} رصيد\",     // ... }      Most projects that use deeply nested keys like 'banner.heading.title' do so out of habit rather than necessity.  A flat key like \"Welcome to our website\" is much better readable, easier to search for, and avoids the overhead of traversing nested objects."},{"title":"Load dictionary using callback","href":"/docs/miscellaneous/load-dictionaries-callback","description":"Learn how to load dictionaries using a callback","content":"Load dictionaries from resource  You can load dictionaries using an (async) callback. This makes it possible to load dictionaries from a database or URL  export const { i18n, useI18n } = await createI18n({     // ...     dictionaries: {         // To get typescript autocompletion replace          // Record with correct typings         nl: async (): Promise> => {             try {                 const request = await fetch('https://example.com/locales/nl');                 const json = await response.json();                  return json;             } catch(err) {                 console.error(err)                 return {};             }   },         // ...     } }); `"}]